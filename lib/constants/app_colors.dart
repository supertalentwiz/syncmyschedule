import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF002B53);
  static const Color accent = Colors.orange;
  static const Color background = Colors.white;
  static const Color textFieldFill = Color(0xFFEEEEEE); // Colors.grey[200]
  static const Color textFieldHint = Color(0xFFB37400); // Orange with opacity
}
